Still on first steps building website, especially when using nodeJs and VS code
using ng new to create a new project
using ng g c to create new components
using ng serve to test
using copilot to fix errors

